//Este programa solicita ao usuário que insira o raio de um círculo,
//calcula a área do círculo e exibe o resultado em uma mensagem de alerta.
const raio = Number(prompt("Digite o raio de um círculo:"))
const area = 3.14 * raio ** 2
alert(`A área do círculo é: ${area}`)