const prompt = require('prompt-sync')();

// Criando um vetor com três números
let numeros = [10, 20, 30];

// Adicionando um número ao final do vetor
numeros.push(40);

// Removendo o primeiro número do vetor
numeros.shift();

// Exibindo o vetor resultante
console.log(numeros);


